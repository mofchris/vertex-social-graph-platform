package com.vertex.notify.domain;

/** The kind of social event a notification represents. */
public enum NotificationType {
    FOLLOW,
    FRIEND_REQUEST,
    FRIEND_ACCEPT,
    POST_LIKE,
    MENTION
}
